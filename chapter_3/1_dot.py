import numpy as np

# Code block 3.1

# define the two vectors
v1 = np.array([2, 5, 4, 7])
v2 = np.array([4, 1, 0, 2])

# dot product of the two vectors
dp = np.dot(v1, v2)

# print the dot product
print(dp)
